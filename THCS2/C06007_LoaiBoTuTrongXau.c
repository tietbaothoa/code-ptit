#include <stdio.h>
#include <math.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>

#define el printf("\n")

int main() {
    char s1[105];
    gets(s1);
    char s2[105];
    gets(s2);
    int len1=strlen(s1);
    int len2=strlen(s2);
    for(int i=0; i<len1; i++){
        
    }
}